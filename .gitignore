# Environment
backend/.env
backend/.env.*
!backend/.env.example

# Python
backend/venv/
backend/env/
**/__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/

# Logs
backend/logs/
*.log

# ML training artifacts (generated locally by scripts/build_ml_dataset.py)
backend/ml_data/

# Raw NCCPL FIPI/LIPI backfill chunks (local fetch artifacts, loaded into
# Postgres by the bulk loader — the DB is the source of truth, not these)
backend/data/nccpl_backfill/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Local Claude Code tooling (e.g. .claude/launch.json — preview-server config)
.claude/
# Downloaded announcement PDFs (PDFParser working files; DB raw_text is the deliverable)
backend/data/announcements/
