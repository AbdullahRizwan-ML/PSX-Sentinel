# PSX Sentinel — Project Context for Claude

> **New session? Read in this order:** this file, then `docs/KNOWN_ISSUES.md`, 
> then the last 3-4 entries of `docs/BUILD_LOG.md`. Do not assume anything 
> works until you've checked the live database yourself — a "complete" status 
> from a previous session or chat history is not verification. Run the actual 
> query. This project has already been burned once by trusting an AI's claim 
> of success without checking the database directly (Phase 2A, May 30).

## What this is

Enterprise-grade AI financial intelligence platform for the Pakistan Stock 
Exchange (PSX). Combines an ML price-direction prediction model with a 
4-agent autonomous research pipeline. (Originally scoped as an earnings 
surprise model — rejected in Phase 3 Session 1, 2026-06-27, since the 
project has no reported-EPS/consensus-estimate data source; see Build Log.) 
This is a portfolio and career-leverage project for a final-year Data 
Science student — it must read as production quality to recruiters and 
technical interviewers, not as a student project.

Name: **PSX Sentinel**. Repository: `github.com/AbdullahRizwan-ML/psx-sentinel`

## Architecture (current, as built)

- **Backend:** FastAPI + SQLAlchemy 2.0 (async) + PostgreSQL (Neon Cloud, free tier) + Redis (Upstash, free tier)
- **Agents:** Custom Python orchestration ONLY — no CrewAI, no LangChain. This was a deliberate reversal from an earlier plan (see Build Log, May 28).
- **LLM routing:** Groq (Llama 3.3-70B versatile) as primary, Gemini 2.0 Flash as fallback. All calls go through a single `LLMGateway` class — this is non-negotiable, see Hard Rules below.
- **Queue:** Celery + Redis (Upstash) for background/nightly jobs
- **Auth:** JWT (access + refresh tokens), bcrypt password hashing
- **Frontend:** Next.js 15 (App Router) + TypeScript + Tailwind + hand-built shadcn/ui-style primitives, in `frontend/` alongside `backend/` in the same monorepo. Custom "Karachi Dusk" design system (deep teal/terracotta/warm cream, Fraunces + Inter type pairing) and a custom `ConvictionDial` SVG gauge as the centerpiece data-viz decision. Explicitly NOT Streamlit — this was rejected early on as looking like a student project.
- **Deployment targets (not yet deployed):** Vercel (frontend), Railway (backend), Neon Cloud (already in use for DB), Upstash (already in use for Redis)

## Hard rules — do not violate these in any future session

1. **Every LLM call goes through `app/core/llm_gateway.py` → `LLMGateway.complete()`.** No agent or collector ever calls the Groq or Gemini SDK directly. This is the single most important architectural rule in the project — it's what gives us cost tracking, circuit breakers, and audit logging for free.
2. **No CrewAI. No LangChain. No Streamlit.** Custom Python orchestration only. This was decided after reading production-engineer feedback (Reddit threads, May 28) showing these frameworks are too heavy/opaque for real deployments.
3. **Everything async.** No blocking I/O in request paths. Sync libraries (yfinance — now removed, feedparser, pdfplumber, playwright) must run via `asyncio.to_thread()`.
4. **Every collector and agent fails gracefully per-item.** One ticker failing must never crash the whole pipeline run. This is enforced via `BaseCollector.run_safe()` and `BaseAgent.run_safe()` — both wrap the actual logic in try/except and always return a result, never raise upward.
5. **Agents skip the LLM call entirely when there's no real data to analyze.** E.g., `NewsSynthesizer` returns a low-confidence result without calling the LLM if there are zero matched articles, rather than hallucinating an analysis from nothing. Same for `FilingSceptic` when no announcement text exists yet.
6. **Data source status (do not re-attempt dead sources without reading KNOWN_ISSUES.md first):**
   - ✅ **PSX DPS timeseries** (`dps.psx.com.pk/timeseries/eod/{ticker}`) — confirmed working, no auth, primary price source.
   - ✅ **ARY News RSS** (`arynews.tv/category/business/feed/`) — confirmed working, primary news source.
   - ❌ **yfinance** — permanently dead. Yahoo Finance blocks PSX `.KA` tickers via Cloudflare. Removed from requirements.txt. Do not reintroduce.
   - ❌ **Dawn Business RSS, Business Recorder RSS** — Cloudflare-blocked (403). Removed.
   - ❌ **PSX announcements JSON/HTML scraping** — PSX portal is JavaScript-rendered; static scraping returns nothing. Deferred to a future Playwright-based PUCARS scraper (not yet built).
   - ⚠️ **CapitalStake API** — looked into as a fundamentals source. No self-serve free tier (consultation-based commercial pricing only). Not pursued. Revisit only if/when productizing commercially with paying subscribers.

## Current build state

- [x] **Phase 1A** — Core infrastructure: config, 10 SQLAlchemy models, JWT security, async Redis client with circuit breaker, `LLMGateway` with Groq→Gemini failover, `BaseAgent` abstract class.
- [x] **Phase 1B** — Application layer: 22 FastAPI endpoints (auth, companies, intelligence, alerts, watchlist, health), Pydantic schemas, Celery scaffolding, Alembic setup, full JWT auth flow verified live.
- [x] **Phase 2A** — Data collectors: `BaseCollector` pattern, company seed data (10 KSE-30 tickers), price collector (PSX DPS), announcement collector (currently returns 0 — portal is JS-rendered), news collector (ARY News RSS), PDF parser (idle — nothing to parse yet), pipeline orchestrator, standalone CLI runner script. **Fixed and verified** after initial yfinance/RSS failures (see Build Log, May 30 – June 8).
- [x] **Phase 2B Session 1** — Four specialist agents built: `TrendAnalyzer`, `NewsSynthesizer`, `FilingSceptic`, `Arbitrator` (`backend/app/agents/`). All inherit `BaseAgent` correctly, all LLM calls go through `self.llm.complete()`, syntax-verified via `ast.parse()`. **Not yet verified end-to-end** — no orchestrator exists yet to actually run them against live data, so behavior at runtime is unconfirmed.
- [x] **Phase 2B Session 2** — `AnalysisOrchestrator` (`backend/app/agents/orchestrator.py`) wires the four agents together: builds `AgentContext` from the live database, runs `TrendAnalyzer`/`NewsSynthesizer`/`FilingSceptic` via `run_safe()`, feeds their output into `Arbitrator`, persists a real `IntelligenceReport`. Wired into both `POST /companies/{ticker}/analyze` (now runs inline and returns the saved report directly) and the Celery `run_analysis` task. **Live-verified** against 2 real tickers (PPL, MCB) — confirmed via direct SQL against `intelligence_reports` and `llm_calls` (not log output): 2 new report rows, 5 new `llm_calls` rows across Groq llama-3.3-70b-versatile, correct LLM skips for `FilingSceptic` (both tickers, 0 calls) and `NewsSynthesizer` (MCB only, 0 articles). None of the four agent files were modified during this session. See Build Log, 2026-06-23 (Session 2 entry) for full detail.
- [x] **Phase 3 Session 1** — ML feature pipeline + labeled dataset (no training yet). Target redefined from earnings surprise to 5-trading-day-ahead price direction (UP/DOWN/FLAT, ±1% flat band) — no EPS/consensus data exists in this project, and the target uses only the already-confirmed-live `daily_prices` data. `backend/app/ml/features.py` computes MA20/MA50/RSI(14)/momentum/volume-trend/volatility/52w-range-position per ticker; `backend/scripts/build_ml_dataset.py` pulls all 10 tickers' price history from the live DB, builds features, and writes a per-ticker chronological 70/15/15 train/val/test split to `backend/ml_data/*.parquet` (gitignored — local training artifact, not application state, not in Postgres). **Live-verified:** 12,025 raw `daily_prices` rows read, 2,560 dropped for insufficient lookback/forward window, 9,465 labeled rows (train 6,621 / val 1,418 / test 1,426), class balance checked per split (no class below 20%), and a no-leakage invariant (`train_max_date < val_min_date < val_max_date < test_min_date`) confirmed per ticker directly against the output files. Two new data-quality issues filed (see Known Issues): ENGRO's shorter history, one suspected unadjusted stock-split row.
- [x] **Phase 3 Session 2** — Data-quality fix (3 unadjusted PSX stock splits in `daily_prices` — MARI 10:1 Sep 2024, LUCK 5:1 Apr 2025, UBL 2:1 Jun 2025 — back-adjusted via new `backend/app/ml/split_adjustments.py`, dataset rebuilt, worst forward-5d return now -25% vs previous -88%). XGBoost multi-class classifier trained (`backend/scripts/train_ml_model.py`, seed=42, model saved to `backend/ml_data/model.json`). **Test accuracy 39.34% vs 33.33% random-chance baseline** — a real but very weak edge; FLAT class structurally unlearned (0 predictions). Leakage checks pass (`close`/`date`/`ticker`/`forward_return_5d` confirmed absent from features; per-ticker chronological invariant holds for all 10 tickers). See Build Log 2026-06-27 (Session 2 entry) for full metrics + Session 3 recommendation.
- [x] **Phase 3 Session 3** — Wired the trained model into `Arbitrator.ml_contribution`: `ML_MAGNITUDE=5.0` (5% max swing), `ML_GATE=0.55` (strict `>` on `max(predict_proba)`). Added `build_features_point_in_time()` (sharing `_compute_indicators` with the batch path) plus `backend/app/ml/inference.py` (module-level model singleton, never raises). Orchestrator extends price-pull window to 600 calendar days, calls `predict_from_prices` before agents run, attaches result to `context.ml_signal` (new field on `AgentContext`), repurposes the legacy `IntelligenceReport.ml_beat_probability` column to hold the UP-class probability. `Arbitrator._build_score_breakdown` emits a rich `ml_detail` sub-dict (predicted_class, max_prob, per-class probabilities, gate_passed, skip_reason, model_caveat) so consumers can distinguish "below gate" / "model unavailable" / "insufficient history". LLM prompt to the Arbitrator's narrative call now includes an explicit weak-signal caveat so generated bull/bear cases don't overstate the model's contribution. **Live-verified** against all 10 tickers (probe) + PPL/MCB/UBL (production orchestrator runs) + UBL with `ML_GATE=0.35` in-process (gate-pass code-path demo): all production runs land at `conviction=58.5, ml_contribution=0.0` because no real ticker clears 0.55 (`max_prob` cluster is [0.357, 0.407] across the universe — the gate is silencing a known-weak signal as designed). Score arithmetic hand-checked for all 4 runs. None of `trend_analyzer.py`, `news_synthesizer.py`, `filing_skeptic.py` were modified.
- [x] **Phase 4 Session 1** — Next.js 15 (App Router) + TypeScript + Tailwind frontend scaffold in `frontend/`, monorepo alongside `backend/` (backend untouched this session). Design system: "Karachi Dusk" palette (deep teal primary, terracotta accent, deep-sage/muted-brick bullish/bearish, warm cream surfaces — `frontend/src/app/globals.css`) and a Fraunces (display/serif) + Inter (body, tabular-nums for figures) type pairing, deliberately not shadcn's stock defaults. The one concrete "point of view" design decision: `ConvictionDial` (`frontend/src/components/conviction-dial.tsx`) — a custom SVG semicircular gauge mapping the 0-100 conviction score to a rotating needle (bearish→neutral→bullish gradient arc) instead of a plain number, so a score reads in under 100ms. Single typed API client chokepoint (`frontend/src/lib/api/`) mirroring the backend's `LLMGateway` philosophy; JWT access+refresh handled in `frontend/src/lib/auth/context.tsx` with auto-refresh-on-401 retry-once. Pages built: login, register, dashboard (10-ticker universe with per-company conviction at a glance, designed empty/loading/error states), company detail (full `IntelligenceReport`: conviction score, bull/bear narrative, ML signal surfaced honestly via `ml_beat_probability` with a low-confidence caveat — `score_breakdown.ml_detail` wasn't exposed by the API yet at the time, see Session 2). **Live-verified**: end-to-end curl-based API integration against the real Neon/Upstash-backed backend (register→login→/me→companies list→company detail→report, values matched documented Build Log figures exactly, e.g. PPL `conviction_score=58.5`, `ml_beat_probability≈0.400`), `npx tsc --noEmit` clean, all 4 routes compiled and served 200 by the Next dev server. Honest gap disclosed at the time: no interactive in-browser visual walkthrough was performed during the session itself (only curl + SSR-shell render checks) — a manual browser pass was done afterward, outside the session, before this was committed. *Doc note: this build-state entry and the matching Build Log entry were backfilled after the fact — the code shipped in commit `504e787` without a doc update; see Build Log for the backfill entry.*
- [x] **Phase 4 Session 2** — `IntelligenceReportResponse` now exposes a typed `score_breakdown` field (`backend/app/schemas/intelligence.py`), hoisted via a `@model_validator(mode="before")` from the persisted `agent_outputs["arbitrator"]["output"]["score_breakdown"]` JSON — no endpoint code changed. Includes a `MlDetail` sub-schema carrying all 3 class probabilities, `gate_passed`, and `skip_reason`. Frontend's company-detail page (`MlSignalCard`) now branches: `MlSignalCardRich` renders the full per-class breakdown with an honest status panel distinguishing `below_confidence_threshold` / `insufficient_history` / `model_unavailable`; `MlSignalCardLegacy` (verbatim copy of the old UP-only component) is the fallback for any report predating this field. **Live-verified** byte-for-byte against the DB for PPL/MCB/UBL (`backend/scripts/verify_score_breakdown.py`) and via the real HTTP API (after a stale-Redis-cache gotcha — see Known Environment Quirks below). `npx tsc --noEmit` clean. None of `trend_analyzer.py`/`news_synthesizer.py`/`filing_skeptic.py`/`orchestrator.py`/`arbitrator.py` were touched — scoring math unchanged. See Build Log, 2026-06-27 (Session 2 entry) for full detail.
- [x] **Phase 4 Session 3** — Price chart on the company detail page (`frontend/src/components/price-chart.tsx`), rendered via `lightweight-charts` v5 (TradingView). Single-canvas, two-pane layout: close-price area line + MA20/MA50 overlay lines on the main pane, volume histogram (tinted by close-vs-previous-close direction) on a synced lower pane sharing the time axis. A 1M/3M/6M/1Y/All preset range selector drives `chart.timeScale().setVisibleRange()` (preset ALL or any preset wider than the available history falls back to `fitContent()` — that's the explicit graceful path for ENGRO's shorter history). Original prompt scoped this as full OHLC candlesticks; rejected in-session because `daily_prices.high`/`low` are *derived* as `max(open, close)`/`min(open, close)` (PSX DPS has no real intraday range — see `docs/KNOWN_ISSUES.md`), so candle wicks would be structurally invisible decoration. A visible caption below the chart states this honestly, same principle as the ML signal card. **Backend change is scoped strictly to the `/prices` endpoint's query window** — bumped the `limit` Query ceiling from 365 → 2000 in `backend/app/api/v1/companies.py` so the chart can pull a full ~2-year ticker history (plus headroom) in a single request. None of `trend_analyzer.py`/`news_synthesizer.py`/`filing_skeptic.py`/`orchestrator.py`/`arbitrator.py` were touched. **Part 2 (MA computation) decision: client-side**, computed via a sliding-window mean in `computeMA()` inside the component itself. Justified because (a) we pull the entire series in one request anyway, (b) the algorithm is identical to `backend/app/ml/features.py::_compute_indicators` (simple unweighted rolling mean of close, `window=20`/`50`, `min_periods=window`), and (c) round-tripping the MAs back through a backend endpoint would have added cost without changing values. **Live-verified**: backend `python backend/scripts/verify_chart_ma.py PPL MCB ENGRO` (which goes through the same `_compute_indicators` path the rest of the system uses) emitted byte-identical MA20/MA50 values to the frontend's `computeMA` algorithm reapplied against the real `/api/v1/companies/{ticker}/prices?limit=2000` JSON — PPL 2026-06-05 `ma20=230.2085, ma50=223.4178`; MCB 2026-06-05 `ma20=406.1105, ma50=398.7368`; ENGRO 2025-01-03 `ma20=426.9010, ma50=366.3388`. `npx tsc --noEmit` exited 0. Next dev server compiled and served HTTP 200 for `/companies/PPL`, `/companies/MCB`, and `/companies/ENGRO` (ENGRO confirms the short-history edge case still loads). **Honest gap flagged**: no in-browser visual walkthrough was performed during the session itself — the Claude in Chrome MCP isn't connected on this machine (`list_connected_browsers` returned `[]`, same gap as Phase 4 Session 2). User should open `/companies/PPL`, `/companies/MCB`, `/companies/ENGRO` in a real browser after `npm run dev` + login to confirm visual treatment and the range selector before merging.
- [x] **Phase 4 Session 4** — Watchlist UI: toggle star on every company card + on the company-detail header, and a single "All companies / My watchlist" segmented control on the dashboard that filters the existing 10-ticker grid client-side. Touches the `/api/v1/watchlist` endpoint family (built in Phase 1B, unexercised since — re-verified live before any frontend code was written, see `backend/scripts/verify_watchlist_endpoints.py`, 13/13 PASS: GET empty, POST 201 with company_name, POST duplicate 409, POST unknown 404, POST lowercase normalizes to UPPER, GET multiple, DELETE 200, DELETE nonexistent 404, DELETE lowercase normalizes, final empty). No backend code changed this session — endpoints worked as documented. Frontend additions: `frontend/src/lib/api/watchlist.ts` (extends the typed client chokepoint with `getWatchlist`/`addToWatchlist`/`removeFromWatchlist`), `frontend/src/lib/watchlist/context.tsx` (`WatchlistProvider` + `useWatchlist`, owns the shared `Set<ticker>` for membership tests so the star toggle and the dashboard filter never split-brain), `frontend/src/components/watchlist-star.tsx` (the visual toggle, terracotta accent fill for "yours" — deliberately *not* primary teal, since teal is already the page's dominant color via KSE-30 chips and ConvictionDial). Optimistic update protocol is concentrated in the provider, not the star: optimistic insert on add → swap with real row on 201 → treat 409 as idempotent success (server already has it) → roll back insert on any other ApiError. Optimistic remove (with the popped row stashed) → treat 404 as idempotent success → restore the stashed row on any other ApiError. Both 409-on-add and 404-on-remove being treated as success-equivalents makes the star tap-idempotent — important because the same star can be tapped twice quickly or on stale data, and neither of those harmless cases should produce an error banner. The dashboard segmented control is purely client-side filtering on the already-fetched universe (the provider's `tickerSet` is the source of truth), so switching tabs is a single React state flip with no API call. Empty "My watchlist" reuses the existing `EmptyState` pattern (same component the "no report yet" CTA established in Session 1) so the visual language stays consistent across the app. **Live-verified** via `backend/scripts/verify_watchlist_flow.py` (15/15 PASS — exercises the same HTTP path the frontend takes: register → list universe → empty watchlist → add PPL → add MCB → client-side intersect with universe → remove PPL → confirm MCB-only → 409 on dup-add → 404 on remove-missing → 404 on unknown-ticker-add → cleanup → final empty), `npx tsc --noEmit` clean, all 3 Next dev-server routes (`/login`, `/dashboard`, `/companies/PPL`) compile and serve HTTP 200. None of `trend_analyzer.py`/`news_synthesizer.py`/`filing_skeptic.py`/`orchestrator.py`/`arbitrator.py` were modified. **Honest gap flagged**: no in-browser visual walkthrough was performed during the session itself — same Claude-in-Chrome-MCP-not-connected gap as Sessions 2 and 3. User should open `/dashboard` in a real browser, tap a few stars, switch to "My watchlist" and confirm the filter + empty state, then visit `/companies/PPL` and confirm the header star, before merging.
- [x] **Phase 4 Session 5** — News article list on the company detail page (`frontend/src/components/news-list.tsx`), rendered below the report body as supporting evidence for the bull/bear narrative the agents produced. Honors the prompt's locked-in design decision ("show only LLM-judged-relevant articles; don't show the noisy raw-keyword-matched set") with a four-state render machine that's honest about the data we actually have: NewsSynthesizer persists an aggregate `relevant_articles` *count*, not per-article flags (and per the hard rules, its LLM prompt can't be changed to start emitting them) — so the matched list is shown only when `relevant_articles == article_count` (LLM judged all of them relevant), and any partial case is held back with a "M of N judged relevant; per-article details aren't surfaced" message rather than picking an arbitrary subset that would falsely imply per-article judgment. The two zero-states the prompt cares about are distinguished cleanly: `article_count == 0` ("No news articles found for [TICKER]" — LLM skipped per the "no real data → no LLM call" rule) vs `article_count > 0 && relevant_articles == 0` ("N articles mentioned [TICKER] but NewsSynthesizer judged none genuinely relevant — likely tangential keyword-match noise"). **Backend change is one additive Pydantic field**: `IntelligenceReportResponse.news_synthesis` (`backend/app/schemas/intelligence.py`), hoisted from `agent_outputs["news_synthesizer"]["output"]` via the existing `@model_validator(mode="before")` (rebuilt to hoist both `score_breakdown` and `news_synthesis` in a single pass; the score_breakdown hoist is regression-tested via `verify_news_synthesis.py` against PPL/MCB/UBL). `news_synthesizer.py` was not touched — only what's serialized out of it. **Live-verified** end-to-end against the real Neon/Upstash backend: `python backend/scripts/verify_news_synthesis.py` confirms byte-identical schema hoists for all 3 tickers with persisted reports, plus zero-state classification (PPL: MATCHED_BUT_NONE_RELEVANT, MCB+UBL: NO_ARTICLES_MATCHED). `python backend/scripts/verify_news_api.py` walks the same HTTP path the frontend takes (register → `/companies/{ticker}/report` → `/companies/{ticker}/news`) and confirms `news_synthesis.article_count == /news.total` for all 3 tickers, plus the render-mode classification matches the DB state exactly. `npx tsc --noEmit` exits 0. Dev server compiles and serves HTTP 200 for `/login`, `/dashboard`, `/companies/PPL` (matched-but-none-relevant), `/companies/MCB` (no-articles-matched), `/companies/UBL` (no-articles-matched). Dev-server processes confirmed terminated and ports 3000/8000 confirmed clear via direct netstat at end of session. None of `trend_analyzer.py`/`news_synthesizer.py`/`filing_skeptic.py`/`orchestrator.py`/`arbitrator.py` were modified. **Honest data gap**: no real example of the partial-relevance or full-relevance render modes exists in the live DB today (every ticker with matched articles is at 0 judged relevant), so those code paths are wired but untestable against current data — flagged in the Build Log. **Honest UI gap**: same Claude-in-Chrome-MCP-not-connected gap as Sessions 2/3/4 — no in-browser walkthrough during this session. User should open `/companies/PPL` (zero-state-2), `/companies/MCB` and `/companies/UBL` (zero-state-1), and any ticker with no report yet (e.g. `HBL`, `LUCK`, `ENGRO`, `MARI`, `MEBL`, `OGDC`, `PSO`; expect the "no relevance judgment yet" CTA) in a real browser before merging.
- [x] **Phase 4 Session 6** — Batched polish pass (100% `frontend/`, zero backend changes), the last item on Phase 4's original scope. **Four sub-steps:** (1) **Unauthenticated landing page** at `/` (`frontend/src/app/page.tsx`, was a bare `redirect("/dashboard")`) — client component that redirects logged-in users to `/dashboard` (same `useAuth()` `{user, loading}` check the login page + `(app)/layout.tsx` use) and otherwise renders a static Karachi-Dusk marketing page: brand, honest framing pulled from this file's "What this is" (ML price-direction model + 4-agent pipeline over KSE-30, no overclaim), primary CTA→`/login`, secondary→`/register`, and the `ConvictionDial` featured decoratively at a static `score=58.5` labelled "Illustrative reading — sign in for live scores" (no fabricated live data). No new deps, no backend calls. (2) **404 page** (`frontend/src/app/not-found.tsx`) — Next's root not-found, styled to match; back-link goes to `/dashboard` (authed) or `/` (not), auth read via `useAuth()`. Plus a distinct **invalid-ticker state** on the company detail page: `/companies/{UNKNOWN}` previously showed a generic "Couldn't load company / try again" `ErrorState` on API 404; now it renders a dedicated "No company found for X" `EmptyState` with a back-to-dashboard link (contained change to `[ticker]/page.tsx`'s existing 404 branch — the `notFound` flag — not a rewrite of its other error states). (3) **Dark mode** — the `.dark` token block already existed in `globals.css` (all role tokens: bg/fg/card/surface/muted/primary/accent/bullish/bearish/neutral/destructive/border/input/ring/shadow; `--radius` intentionally cascades from `:root`, unchanged by theme), and `tailwind.config.ts` was already `darkMode: ["class"]`, so this session added only the *mechanism*: `frontend/src/lib/theme/context.tsx` (`ThemeProvider`/`useTheme`, toggles `.dark` on `<html>`, persists to `localStorage['psx-theme']`, `prefers-color-scheme` default), `frontend/src/components/theme-toggle.tsx` (icon variant for the desktop NavBar, `labeled` variant for the mobile drawer), a no-flash inline `<head>` script + `suppressHydrationWarning` on `<html>` in `layout.tsx`, and `ThemeProvider` wrapping `AuthProvider` at the root. (4) **Mobile nav drawer** — `NavBar` was desktop-only (nav links `hidden sm:flex`, so the Dashboard link vanished below 640px); rewrote it so at `<sm` the desktop nav + account menu are `hidden` and a `sm:hidden` hamburger opens a full-height right-side drawer (`role="dialog"`, backdrop, Escape-to-dismiss, body-scroll-lock, close-button focus-on-open) containing the same destinations (Dashboard), the labeled theme toggle, and the account block + Sign out. Breakpoint chosen as `sm` (640px) to match the existing nav's responsive boundary. **Two real bugs caught during live in-browser verification and fixed in-session:** (a) the no-flash script caused a React hydration mismatch on `<html className>` → fixed with `suppressHydrationWarning` (the idiomatic next-themes approach); (b) the drawer was initially rendered *inside* `<header>`, whose `backdrop-blur-md` establishes a containing block for `position:fixed` descendants, clamping the drawer's `h-full`/backdrop `inset-0` to the 64px header box → fixed by rendering `<MobileDrawer>` as a sibling of `<header>` (fragment root) so the fixed panel + backdrop size against the viewport (verified: panel computed height went 64px→812px). **Live-verified in a real browser** (Claude Preview MCP — this session finally closed the in-browser-walkthrough gap flagged in Sessions 2–5, via `.claude/launch.json` [untracked local tooling, not committed]): landing renders unauthenticated in both light + dark; the theme toggle live-swaps the whole page *without reload* and persists to localStorage; `prefers-color-scheme:dark` default confirmed; 404 renders (`GET /this-does-not-exist` → real HTTP **404**; `/companies/ZZZNOTATICKER` → 200 route + client-side not-found card); mobile drawer at 375px confirmed via computed styles (desktop nav `display:none`, hamburger visible) + opens full-height with backdrop + Escape closes + scroll-lock releases; `npx tsc --noEmit` exits 0. **Dark-mode staleness finding (required deliverable, NOT fixed per prompt hard rules):** `ConvictionDial` is **toggle-safe** — it passes `hsl(var(--token))` *strings* as live SVG attributes, which the browser re-resolves at paint, so it re-themes instantly on toggle (empirically confirmed: dial showed correct light colors after a dark→light toggle with no reload). `PriceChart` **goes stale on toggle** — it reads CSS vars via `getComputedStyle` inside a `useEffect` keyed only on `[prices]`, so a theme toggle doesn't re-run `createChart` and the line/axis/grid colors keep the mount-time theme until the next reload/remount (empirically confirmed: post-toggle-to-dark the chart kept dim light-derived colors; a fresh reload in dark rendered it correctly). Flagged as a follow-up (would require touching `price-chart.tsx` internals — out of scope this session). **Ports 3000/8000 confirmed clear via literal netstat at session end.** None of `trend_analyzer.py`/`news_synthesizer.py`/`filing_skeptic.py`/`orchestrator.py`/`arbitrator.py` touched — the entire diff is under `frontend/`. See Build Log, 2026-07-03 for the per-sub-step detail.
- [x] **Phase 5 Session 1** — First-ever backtest of the trained XGBoost price-direction model (`backend/scripts/backtest_xgboost.py`), opening Phase 5 ("Signal validation & data depth"). Read-only, `vectorbt==1.0.0`-based, **test-split-only** (loads `ml_data/test.parquet` + `model.json` as-is, **no retrain**, no DB writes). Answers the never-before-asked question: net of realistic PSX costs, would trading the model's historical predictions have made money? **Two rules side-by-side, long-only, close-to-close, equal-weight, one position/ticker, no leverage:** *ungated* (long every UP prediction) vs *gated* (`max_prob > 0.55`, matching `Arbitrator.ML_GATE`). Trade rule is a "regime hold" (one long position while the model stays bullish, exit at close on flip) rather than overlapping fixed-5-day holds — per-signal 5-day holds would need concurrent same-ticker positions (pyramiding), which the no-leverage rule forbids. **Costs (documented, not arbitrary):** 0.15% commission/side (0.30% round-trip, bundling brokerage + FED/sales-tax-on-commission + CDC/SECP/PSX/NCCPL levies, deliberately expensive) applied via vectorbt `fees`; plus 15% CGT on net realised gain applied post-hoc at sleeve level (filer, <12-mo hold), applied identically to buy-and-hold; slippage not modelled (close-to-close), flagged optimistic; 0.05%/side discount-broker sensitivity reported alongside. **Headline (9-ticker equal-weight sleeve, shared window 2025-10-24→2026-05-29, net 0.30% round-trip, pre-CGT):** ungated **+5.10%** (Sharpe +0.43, maxDD −21.30%, win 63.49%, 63 trades) vs buy-and-hold **+3.33%** (Sharpe +0.33, maxDD −24.41%); gated **+2.35%** (Sharpe +0.42, maxDD **−5.70%**, win 63.64%, 22 trades — its value is risk reduction from sitting in cash, not return). Post-CGT: ungated +4.33%, gated +2.00%, B&H +2.83%. **Honest read:** a real but *thin* edge (Sharpe well under 1, drawdown nearly as deep as B&H, per-ticker spread PSO −20% to UBL +24%) — validates the signal is "not worthless after costs", does NOT establish a deployable strategy. **No-leakage proven not assumed:** harness asserts `test_min > val_max` per ticker and the boundaries were pasted side-by-side with `verify_dataset.py`'s independently-derived split — exact match for all 10 tickers; the 5-day forward label is never a trading input (trades come from `predict_proba` of backward-looking features only). ENGRO quarantined from the sleeve (its 2024 test window is disjoint from the other 9's 2025-26 window) but fully reported per-ticker (ungated +53.61% on a short lucky run — explicitly flagged non-representative). 105 of 1,426 test rows clear the 0.55 gate (vs zero on the single-latest-day production probe, since the test set spans 1,426 historical days). Added `vectorbt==1.0.0` to `requirements.txt` — pulls numba≥0.66/llvmlite 0.48 which support the existing `numpy==2.1.3` pin (**numpy NOT downgraded** — verified). Full numbers in new `docs/BACKTEST_RESULTS.md`. None of `trend_analyzer.py`/`news_synthesizer.py`/`filing_skeptic.py`/`orchestrator.py`/`arbitrator.py` touched. See Build Log, 2026-07-04 (Phase 5 Session 1).
- [ ] **Future / deferred** — Playwright-based PUCARS announcement/PDF scraper; possible CapitalStake integration if productized; possible additional news sources (Dunya News website scraping, since their RSS is blocked but the site itself loads). *(`PriceChart` dark-mode re-theming, previously listed here, was **fixed 2026-07-04** — the chart now re-themes in place on live toggle via `useTheme()` + `applyOptions()`/`setData()`, preserving zoom/range; see the Hotfix entry in BUILD_LOG.md and the resolved entry in `docs/KNOWN_ISSUES.md`.)*

## Database — verified live state (Neon Cloud PostgreSQL)

*Last manually verified: June 8, 2026, via direct SQL query — not just pipeline log claims.*

| Table | State |
|---|---|
| `companies` | 10 rows seeded (ENGRO, LUCK, OGDC, PPL, MCB, HBL, UBL, MARI, PSO, MEBL) |
| `daily_prices` | 9,904 rows total across all 10 tickers, ~2 years of history each, 0 collection failures on last full run |
| `news_articles` | 19 rows, all from ARY News, matched against PPL/PSO/OGDC (note: matching is keyword-based and somewhat noisy — see Known Issues) |
| `announcements` | 0 rows — PSX portal scraping not yet functional |
| `intelligence_reports` | 0 rows — Phase 2B not yet built |
| `llm_calls` | 0 rows — no agent has made a real LLM call yet |
| `pipeline_runs` | Multiple clean `SUCCESS` records for `price_collector` and `news_collector` as of the June 8 full run |

**Ticker universe (KSE-30 subset, 10 tickers):** `ENGRO, LUCK, OGDC, PPL, MCB, HBL, UBL, MARI, PSO, MEBL`

## Key files map

| File | Purpose |
|---|---|
| `backend/app/core/config.py` | `get_settings()` singleton, all env vars |
| `backend/app/core/security.py` | JWT creation/verification, password hashing, FastAPI auth dependencies |
| `backend/app/core/redis_client.py` | Async Redis wrapper: caching, rate limiting, circuit breaker state |
| `backend/app/core/llm_gateway.py` | **The most important file.** Single chokepoint for every LLM call. Groq primary, Gemini fallback, circuit breaker, PostgreSQL audit logging via `LLMCall` |
| `backend/app/db/models.py` | All 10 SQLAlchemy models — source of truth for schema |
| `backend/app/db/session.py` | Async engine, `get_db()` FastAPI dependency, `init_db()` |
| `backend/app/agents/base.py` | `AgentContext`, `AgentResult`, `BaseAgent` abstract class with `run()` / `run_safe()` contract |
| `backend/app/agents/orchestrator.py` | `AnalysisOrchestrator` — wires the 4 agents together, builds `AgentContext` from the DB, persists `IntelligenceReport`. Creates the report row before any agent runs so `LLMCall.analysis_id` has a valid FK target |
| `backend/app/collectors/base_collector.py` | `BaseCollector` — same pattern as `BaseAgent`, for data ingestion jobs |
| `backend/app/collectors/pipeline.py` | Orchestrates seed → prices → announcements → PDFs → news, in sequence, per-stage error isolation |
| `backend/app/collectors/price_collector.py` | PSX DPS timeseries fetcher (yfinance removed — see Known Issues) |
| `backend/app/collectors/news_collector.py` | ARY News RSS fetcher with XML-cleaning preprocessing |
| `backend/app/api/v1/companies.py` | `POST /companies/{ticker}/analyze` runs `AnalysisOrchestrator` **inline** and returns the saved `IntelligenceReportResponse` directly (changed in Phase 2B Session 2 — previously queued a Celery task and returned 202) |
| `backend/app/workers/tasks.py` | Celery tasks — `run_analysis` now calls `AnalysisOrchestrator` (Phase 2B Session 2; previously a placeholder that called the data-collection pipeline). Used for nightly/background runs; shares the same orchestrator code path as the API endpoint |
| `backend/scripts/run_pipeline.py` | Standalone CLI runner for manual pipeline testing (`--seed-only`, `--tickers X,Y`) |
| `backend/scripts/diagnose_sources.py` | Diagnostic script that tests each external data source without touching the DB |
| `backend/scripts/test_analysis.py` | Standalone verification script for `AnalysisOrchestrator` — runs real tickers and queries `intelligence_reports`/`llm_calls` directly to confirm persistence |
| `backend/app/ml/features.py` | Phase 3 feature pipeline — `build_features()` (batch, returns a labeled DataFrame for offline training) and `build_features_point_in_time()` (Session 3, returns the latest 11-feature vector as a dict for live inference, or `None` if insufficient trailing history). Both share `_compute_indicators()` so the live vector is identical to what the model was trained on. Pure pandas |
| `backend/app/ml/inference.py` | Phase 3 Session 3 — live inference for the Arbitrator. Lazy-loads `ml_data/model.json` once at module level, then `predict_from_prices(prices, threshold)` returns a rich dict (`available`, `gate_passed`, `skip_reason`, `predicted_class`, `max_prob`, `probabilities`, `as_of_date`). Never raises — degradation is signalled in the result dict. Imported by `orchestrator.py`; not imported by `features.py` so the offline path stays import-free |
| `backend/scripts/probe_ml_signal.py` | Read-only diagnostic — runs `predict_from_prices` against every ticker in the DB and prints class/prob/gate-status. No LLM calls, no writes. Use to check "is the gate doing anything?" before/after any model retrain |
| `backend/scripts/backtest_xgboost.py` | Phase 5 Session 1 — the out-of-sample backtest harness (`vectorbt==1.0.0`). Read-only: loads `ml_data/model.json` + `ml_data/test.parquet` (test split ONLY, no retrain, no DB writes), runs gated (`max_prob>0.55`, matches `Arbitrator.ML_GATE`) and ungated long-only close-to-close strategies, reports total return / Sharpe / max drawdown / win rate / trade count vs a buy-and-hold benchmark, net of PSX transaction costs (0.15%/side commission + 15% CGT post-hoc). Asserts `test_min>val_max` per ticker (no-leakage). Headline aggregate = 9-ticker equal-weight sleeve; ENGRO reported separately (disjoint 2024 window). Results written up in `docs/BACKTEST_RESULTS.md`. Re-run after any model retrain |
| `backend/scripts/verify_ml_wiring.py` | Phase 3 Session 3 verification script — 4 parts (probe / production orchestrator run on PPL+MCB+UBL / gate-pass code-path demo with ML_GATE temporarily lowered to 0.35 / direct SQL dump of `score_breakdown.ml_detail`). DOES write IntelligenceReport rows. Restores production constants on exit |
| `backend/app/ml/split_adjustments.py` | Phase 3 Session 2 — `SPLIT_ADJUSTMENTS` table + `apply_split_adjustments()` helper. PSX DPS doesn't adjust raw closes for stock splits — this back-adjusts pre-split close/open/high/low/volume by an empirical ratio so the price series is continuous across split days. Currently covers MARI 2024-09-16, LUCK 2025-04-28, UBL 2025-06-23. Add new rows here if new splits show up |
| `backend/scripts/build_ml_dataset.py` | Phase 3 dataset builder — pulls all 10 tickers from the live DB, applies `apply_split_adjustments()`, runs `features.py` per ticker, does a per-ticker chronological 70/15/15 split, writes `backend/ml_data/{train,val,test}.parquet` (gitignored) |
| `backend/scripts/train_ml_model.py` | Phase 3 Session 2 — XGBoost multi-class trainer + evaluator. Loads the parquet files, fits with early stopping on val, reports test accuracy / per-class P-R-F1 / confusion matrix / feature importances, saves `ml_data/model.json` and `ml_data/metrics.json`. Re-runnable end-to-end, seed=42 |
| `backend/scripts/find_split_row.py` | Read-only diagnostic — prints top extreme single-day and 5-day-forward returns across all tickers. Use to discover any new unadjusted corporate actions before adding them to `split_adjustments.py` |
| `backend/scripts/verify_dataset.py` | Read-only sanity check on the parquet files — most-extreme returns per split + per-ticker chronological-invariant check |
| `frontend/src/lib/api/client.ts` | Phase 4 Session 1 — the frontend's single API-client chokepoint (mirrors `LLMGateway`'s philosophy). `apiRequest<T>()` plus `tokenStorage`/`ApiError`; auto-refreshes the access token on a 401 and retries once (deduped via a shared in-flight `refreshPromise`). No component calls `fetch()` directly — `lib/api/auth.ts`, `lib/api/companies.ts`, and `lib/api/watchlist.ts` (Phase 4 Session 4) are the only callers |
| `frontend/src/lib/auth/context.tsx` | Phase 4 Session 1 — `AuthProvider`/`useAuth()`, the JWT access+refresh session state for the whole app. Auth-gated route group `(app)` redirects to `/login` when this reports no user |
| `frontend/src/app/page.tsx` | Phase 4 Session 6 — the unauthenticated landing page at `/` (was a bare `redirect("/dashboard")`). Client component: redirects logged-in users to `/dashboard`, otherwise renders a static Karachi-Dusk marketing page (honest framing from "What this is", CTAs to `/login`+`/register`, a decorative `ConvictionDial` at a static illustrative score). No backend calls |
| `frontend/src/app/not-found.tsx` | Phase 4 Session 6 — root 404 page, styled to match. Back-link resolves to `/dashboard` (authed) or `/` (not) via `useAuth()`. Note the invalid-*ticker* case is handled separately, inside `(app)/companies/[ticker]/page.tsx`'s 404 branch (a dedicated `EmptyState`), not here |
| `frontend/src/lib/theme/context.tsx` | Phase 4 Session 6 — `ThemeProvider`/`useTheme()`. Toggles the `.dark` class on `<html>` (matches Tailwind `darkMode: ["class"]` + the existing `.dark` token block in `globals.css`), persists to `localStorage['psx-theme']`, defaults to `prefers-color-scheme`. Reconciles its React state from the class the no-flash `<head>` script already applied, so no hydration mismatch. Wraps `AuthProvider` at the root |
| `frontend/src/components/theme-toggle.tsx` | Phase 4 Session 6 — `ThemeToggle`. Two presentations: default icon button (desktop `NavBar`) and `labeled` full-width row (mobile drawer). Shows the theme you'd switch *to* (moon in light, sun in dark) |
| `frontend/src/lib/api/watchlist.ts` | Phase 4 Session 4 — typed `getWatchlist`/`addToWatchlist`/`removeFromWatchlist` over the live-verified `/api/v1/watchlist` family. Documents the actual edge-case responses (201 on add / 409 on duplicate / 404 on unknown ticker; 200 on remove / 404 on already-gone), so the watchlist context can map them to its idempotent-success vs rollback branches without guessing |
| `frontend/src/lib/watchlist/context.tsx` | Phase 4 Session 4 — `WatchlistProvider`/`useWatchlist()`, the source-of-truth for watchlist state across the auth-gated `(app)` route group. Exposes a `Set<string>` of uppercase tickers for O(1) membership tests (the dashboard filter and every `WatchlistStar` use this together), plus `add`/`remove`/`toggle` actions. Owns the optimistic-update protocol: optimistic insert/remove → swap with real server payload on success → treat 409-on-add and 404-on-remove as idempotent success → roll back on any other ApiError, surfaced to the caller via a thrown ApiError. The idempotence makes the star tap-safe |
| `frontend/src/components/watchlist-star.tsx` | Phase 4 Session 4 — `WatchlistStar`, the visual toggle. Two size variants: `card` (small, sits in the top-right of a dashboard card, defaults `stopPropagation=true` so taps don't navigate into the wrapping `Link`) and `header` (slightly larger, sits next to the H1 on the company detail page, `stopPropagation={false}` since the header isn't a link). Filled accent (terracotta) for "yours" / outlined muted for "not yours" — deliberately not primary teal, since teal is already the page's dominant color via the KSE-30 chips, conviction dial, and link-hover accents. Delegates the entire optimistic-update story to the `WatchlistProvider`; just calls `toggle()` and waits |
| `frontend/src/components/conviction-dial.tsx` | Phase 4 Session 1 — `ConvictionDial`, the custom SVG gauge that is this project's deliberate "point of view" data-viz decision (see Phase 4 Session 1 build-state entry above for the reasoning). Used at small size on dashboard cards and large size on the company detail header |
| `frontend/src/app/(app)/layout.tsx` | Phase 4 Session 1 — auth-gated layout: shows a loader while `useAuth()` settles, redirects to `/login` on `!user`, otherwise renders `<NavBar />` + `<main>` inside a container. Phase 4 Session 4 wrapped the gated subtree in `<WatchlistProvider>` so every authenticated page can read shared watchlist state without per-page fetches |
| `frontend/src/app/(app)/dashboard/page.tsx` | Phase 4 Session 1 — lists the 10-ticker universe, fetches `listCompanies` then per-ticker `getCompanyDetail` in parallel, plus a best-effort `getMarketSummary` strip. Designed loading-skeleton/error states, no flash-of-nothing. Phase 4 Session 4 added an "All companies / My watchlist" segmented control (`DashboardTabs`) that filters the existing company grid client-side via the `WatchlistProvider`'s `tickerSet`, plus a designed empty state on the watchlist tab when the user has no entries yet (reuses the same `EmptyState` pattern as the Session 1 "no report yet" CTA, so visual language stays consistent) |
| `frontend/src/components/company-card.tsx` | Phase 4 Session 1 — `CompanyCard`, the dashboard card. Phase 4 Session 4 added a `WatchlistStar` (`variant="card"`) to the card's top-right, beside the `ArrowUpRight` link indicator |
| `frontend/src/app/(app)/companies/[ticker]/page.tsx` | Phase 4 Session 1 — company detail page: latest `IntelligenceReport` in full (conviction score via `ConvictionDial`, bull/bear narrative, risk factors, ML signal). Houses `MlSignalCard`, which Phase 4 Session 2 split into `MlSignalCardRich`/`MlSignalCardLegacy` (see `backend/app/schemas/intelligence.py` row below). Phase 4 Session 3 wired the new `PriceChart` in directly under the company header. Phase 4 Session 4 placed a `WatchlistStar` (`variant="header"`) directly to the right of the ticker H1. Phase 4 Session 5 wired the new `NewsList` in after the report body (or after the "no report yet" CTA) so it reads as supporting evidence for the bull/bear narrative. Phase 4 Session 6 added a distinct invalid-ticker state: on API 404 it now renders a dedicated "No company found" `EmptyState` (with a back-to-dashboard link) via a `notFound` flag, instead of the generic retry `ErrorState` |
| `frontend/src/components/nav-bar.tsx` | Phase 4 Session 1 — top nav for the auth-gated app: brand, Dashboard link, account dropdown (user info + Sign out). Phase 4 Session 6 added the desktop `ThemeToggle` and a mobile `MobileDrawer`: below `sm` (640px) the desktop nav + account menu are hidden and a hamburger opens a full-height right drawer (Dashboard link, labeled theme toggle, account block + Sign out; `role="dialog"`, backdrop, Escape-to-dismiss, scroll-lock). The drawer is rendered as a **sibling** of `<header>`, not a child — the header's `backdrop-blur-md` would otherwise clamp the fixed panel's `h-full`/`inset-0` to the 64px header box |
| `frontend/src/components/price-chart.tsx` | Phase 4 Session 3 — `PriceChart`, the close-price + MA20/MA50 + volume chart on the company detail page, built on `lightweight-charts` v5. Self-contained: pulls its own series via `getCompanyPriceHistory`, computes MAs client-side via `computeMA()` (sliding-window mean — algorithm matches `backend/app/ml/features.py::_compute_indicators` byte-for-byte; verified live, see Phase 4 Session 3 build state). Pane 0 = price area line + two MA overlay lines; pane 1 = volume histogram, both panes share the time axis. Includes a 1M/3M/6M/1Y/All range selector (graceful fallback to `fitContent()` if the requested range exceeds the ticker's available history — that's the ENGRO-short-history path). Visible caption explains the close-line-vs-candlestick decision honestly |
| `backend/app/schemas/intelligence.py` | Pydantic v2 response/request schemas for intelligence reports, alerts, and watchlist endpoints. `IntelligenceReportResponse.score_breakdown` (Phase 4 Session 2) and `IntelligenceReportResponse.news_synthesis` (Phase 4 Session 5) are both hoisted out of the persisted `agent_outputs` JSON via a single `@model_validator(mode="before")` (rebuilt in Session 5 to do both in one pass; the score_breakdown hoist is regression-tested by `verify_news_synthesis.py`). See `MlDetail`/`ScoreBreakdown` for the per-class ML probabilities + gate status, and `NewsSynthesis` for the sentiment/article_count/relevant_articles aggregates that disambiguate the two zero-states the news list renders. `WatchlistItemResponse` and `AddWatchlistRequest` (Phase 1B, exercised live for the first time in Phase 4 Session 4) drive the `/watchlist` endpoint family |
| `frontend/src/components/news-list.tsx` | Phase 4 Session 5 — `NewsList`, the relevant-articles card on the company detail page. State machine over the combined data (`/companies/{ticker}/news` paginated articles + `IntelligenceReportResponse.news_synthesis` aggregate) produces seven render modes (loading / error / no-report-yet / no-articles-matched / matched-but-none-relevant / partial-relevance / full-relevance). Strictly honors the locked design decision ("show only judged-relevant articles, don't show the noisy raw set") by rendering the matched list only when `relevant_articles === article_count` (i.e. the LLM judged all matched articles relevant); the partial-relevance case is held back with an explanatory message rather than fabricating per-article judgments NewsSynthesizer doesn't currently emit. Self-contained data fetching via `getCompanyNews` (typed-client chokepoint), no per-page wiring needed. Article rows link out to the original source in a new tab when a URL is present |
| `backend/scripts/verify_news_synthesis.py` | Phase 4 Session 5 — read-only diagnostic that cross-checks the schema-hoisted `news_synthesis` against the raw `agent_outputs["news_synthesizer"]["output"]` per ticker, AND regression-checks the existing `score_breakdown` hoist still produces identical values. Also classifies each ticker's zero-state (NO_ARTICLES_MATCHED / MATCHED_BUT_NONE_RELEVANT / HAS_RELEVANT) so the frontend render-mode logic can be hand-compared against the DB state. Run after any change to `IntelligenceReportResponse`'s validator |
| `backend/scripts/verify_news_api.py` | Phase 4 Session 5 — read-write smoke test that exercises the exact HTTP path the frontend `NewsList` takes. Busts today's Redis report-cache for the test tickers, registers a fresh timestamped user, fetches `/companies/{ticker}/report` (asserts `news_synthesis` and `score_breakdown` both present), fetches `/companies/{ticker}/news`, cross-checks `news_synthesis.article_count == /news.total`. Confirms backend serves what the frontend needs to render every zero-state correctly. PPL/MCB/UBL baseline: all PASS |
| `backend/scripts/inspect_news_state.py` | Phase 4 Session 5 — read-only diagnostic that prints, per ticker, the `news_articles` row count and the latest `IntelligenceReport`'s `news_synthesizer.output` summary. Used at the start of any session that touches news rendering to confirm what zero-states currently exist in the live data (so you know which render paths are testable against real DB content) |
| `backend/scripts/verify_score_breakdown.py` | Phase 4 Session 2 — read-only diagnostic that cross-checks the API-served `score_breakdown` (via `IntelligenceReportResponse.model_validate`) against the raw `agent_outputs` column in the live DB, per ticker |
| `backend/scripts/verify_chart_ma.py` | Phase 4 Session 3 — read-only diagnostic that prints MA20/MA50 for the last 3 trading days per ticker via the canonical backend path (`_compute_indicators`), for hand-comparison with the frontend's client-side MA computation. Prints both raw and split-adjusted series so the MA values you see in the rendered chart (raw) can be told apart from the ML pipeline's values (adjusted) for tickers with split events |
| `backend/scripts/verify_watchlist_endpoints.py` | Phase 4 Session 4 — read-write smoke test for the `/api/v1/watchlist` endpoint family. Registers a fresh timestamped user, then walks every documented status code: GET empty, POST 201 (with `company_name` joined), POST 409 duplicate, POST 404 unknown ticker, POST lowercase normalizes to UPPER, GET multiple, DELETE 200, DELETE 404 already-gone, DELETE lowercase normalizes, GET empty again. 13 PASS / 0 FAIL was the Session 4 baseline. Run this before relying on the endpoints in any new code |
| `backend/scripts/verify_watchlist_flow.py` | Phase 4 Session 4 — read-write end-to-end flow verification mirroring the actual frontend user journey: dashboard mount → empty watchlist → star PPL → star MCB → client-side intersect-with-universe (the "My watchlist" tab filter) → unstar PPL → confirm MCB still present → dry-runs the 409-on-dup-add and 404-on-remove-missing branches the `WatchlistProvider` treats as idempotent success → dry-runs the 404-on-unknown-ticker rollback branch → cleanup. 15 PASS / 0 FAIL was the Session 4 baseline |

## Environment / secrets (never commit these — they live in `.env`, gitignored)

`DATABASE_URL` (Neon Cloud, asyncpg), `SECRET_KEY`, `GROQ_API_KEY`, `GEMINI_API_KEY`, 
`REDIS_URL` (Upstash, `rediss://`), `CELERY_BROKER_URL` / `CELERY_RESULT_BACKEND` 
(also pointed at Upstash, db indexes 1/2), `PSX_TICKERS`, `NIGHTLY_PIPELINE_HOUR`, 
`FRONTEND_URL`. Template lives in `backend/.env.example` (tracked in git).

## Known environment quirks (full detail in `docs/KNOWN_ISSUES.md`)

- `bcrypt` must stay pinned to `4.0.1` — `passlib==1.7.4` breaks with bcrypt 4.1+.
- Neon Cloud's SSL connection string (`sslmode=require&channel_binding=require`) is not 
  directly compatible with asyncpg's URL parser — handled via custom logic in `session.py`.
- Windows terminal (cp1252) can't render the `✓` unicode character in logs — replaced with `[OK]`.
- `GET /companies/{ticker}/report` is cached in Redis for 86400s (`REPORT_CACHE_KEY = "report:{ticker}:{date}"`). Any schema change to `IntelligenceReportResponse` (e.g. Phase 4 Session 2's `score_breakdown` field) will not appear in already-cached responses until the TTL expires — manually `DEL report:*` on Upstash after deploying a response-schema change if you need it visible immediately, otherwise expect a up-to-24h window where the API serves the pre-change shape (frontend's legacy fallback handles this gracefully).

## Working conventions for this project

- Every phase ends with a **live verification step against the actual database**, not 
  just trusting a completion summary. This is a hard-learned lesson from Phase 2A (see 
  Build Log, May 30) where a session claimed success but had silently failed on 3 of 4 
  data sources.
- Antigravity sessions were split into smaller chunks (e.g. "Phase 1A" vs "Phase 1B") 
  specifically to avoid the AI cutting corners on later files in a long single-shot prompt.
- When a phase prompt is run, it should always end by asking for a structured completion 
  summary (files generated, what was verified, what's still a placeholder, what the next 
  phase needs) — this pattern has worked well and should continue.
- This project is being actively shifted from Antigravity (Gemini 3.1 Pro / Claude via 
  Antigravity) to Claude Code directly, partly because Antigravity sessions lost context 
  and hit model quota limits mid-execution more than once.